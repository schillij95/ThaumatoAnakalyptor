# @package _group_
- _target_: pytorch_lightning.callbacks.ModelCheckpoint
  monitor: train_loss_mean
  save_last: true
  save_top_k: 1
  mode: min
  dirpath: ${general.save_dir}
  filename: "{epoch}-{train_loss_mean:.3f}"
  every_n_epochs: 1

- _target_: pytorch_lightning.callbacks.LearningRateMonitor
